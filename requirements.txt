numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.1
# 4.41 renamed TrainingArguments.evaluation_strategy -> eval_strategy;
# the old name was removed in 4.46, so this lower bound is required.
transformers>=4.41
accelerate>=0.26.0
tqdm>=4.65
pyyaml>=6.0
